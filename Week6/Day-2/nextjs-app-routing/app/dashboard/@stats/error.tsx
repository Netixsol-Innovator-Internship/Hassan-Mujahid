"use client";

const error = () => {
  return <div>An error occured while loading stats Page!</div>; //This is the page that get's rendered automatically by NEXTJS only when an error occurs while loading stats page
};

export default error;
